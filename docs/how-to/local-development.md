# Local development

1. Install Node.js 24 and run `npm ci`.
2. Change the authoritative source below `src/` and the corresponding tests.
3. Run `npm test` and `npm run build` before opening a pull request.

Keep repository-specific secrets outside Git and preserve generated-file boundaries.
